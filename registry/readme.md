# Registry utils

open the files to install

## OpenWithZed

- Build Zed for Windows from source
- Place the output (zed.exe) in "C:\Program Files\ZedRelease\"
- (Extra) Add the folder to path or install the CLI from the app to use it in the terminal
- Run OpenWithZed.reg to add context menu options for it
